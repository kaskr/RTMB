## Autogenerated - do not edit

setMethod("dexp",
signature(x = "advector_castable", rate = "advector_castable", log = "logical"),
function( x, rate, log ) {
x <-  advector ( x )
rate <-  advector ( rate )
log <-  as.logical ( log )
distr_dexp ( x, rate, log )
}
)
setMethod("dweibull",
signature(x = "advector_castable", shape = "advector_castable", scale = "advector_castable", log = "logical"),
function( x, shape, scale, log ) {
x <-  advector ( x )
shape <-  advector ( shape )
scale <-  advector ( scale )
log <-  as.logical ( log )
distr_dweibull ( x, shape, scale, log )
}
)
setMethod("dbinom",
signature(x = "advector_castable", size = "advector_castable", prob = "advector_castable", log = "logical"),
function( x, size, prob, log ) {
x <-  advector ( x )
size <-  advector ( size )
prob <-  advector ( prob )
log <-  as.logical ( log )
distr_dbinom ( x, size, prob, log )
}
)
setMethod("dbeta",
signature(x = "advector_castable", shape1 = "advector_castable", shape2 = "advector_castable", ncp = "missing", log = "logical"),
function( x, shape1, shape2, log ) {
x <-  advector ( x )
shape1 <-  advector ( shape1 )
shape2 <-  advector ( shape2 )
log <-  as.logical ( log )
distr_dbeta ( x, shape1, shape2, log )
}
)
setMethod("df",
signature(x = "advector_castable", df1 = "advector_castable", df2 = "advector_castable", ncp = "missing", log = "logical"),
function( x, df1, df2, log ) {
x <-  advector ( x )
df1 <-  advector ( df1 )
df2 <-  advector ( df2 )
log <-  as.logical ( log )
distr_df ( x, df1, df2, log )
}
)
setMethod("dlogis",
signature(x = "advector_castable", location = "advector_castable", scale = "advector_castable", log = "logical"),
function( x, location, scale, log ) {
x <-  advector ( x )
location <-  advector ( location )
scale <-  advector ( scale )
log <-  as.logical ( log )
distr_dlogis ( x, location, scale, log )
}
)
setMethod("dt",
signature(x = "advector_castable", df = "advector_castable", ncp = "missing", log = "logical"),
function( x, df, log ) {
x <-  advector ( x )
df <-  advector ( df )
log <-  as.logical ( log )
distr_dt ( x, df, log )
}
)
setMethod("dnorm",
signature(x = "advector_castable", mean = "advector_castable", sd = "advector_castable", log = "logical"),
function( x, mean, sd, log ) {
x <-  advector ( x )
mean <-  advector ( mean )
sd <-  advector ( sd )
log <-  as.logical ( log )
distr_dnorm ( x, mean, sd, log )
}
)
setMethod("dnbinom",
signature(x = "advector_castable", size = "advector_castable", prob = "advector_castable", mu = "missing", log = "logical"),
function( x, size, prob, log ) {
x <-  advector ( x )
size <-  advector ( size )
prob <-  advector ( prob )
log <-  as.logical ( log )
distr_dnbinom ( x, size, prob, log )
}
)
setMethod("dpois",
signature(x = "advector_castable", lambda = "advector_castable", log = "logical"),
function( x, lambda, log ) {
x <-  advector ( x )
lambda <-  advector ( lambda )
log <-  as.logical ( log )
distr_dpois ( x, lambda, log )
}
)
setMethod("dgamma",
signature(x = "advector_castable", shape = "advector_castable", rate = "missing", scale = "advector_castable", log = "logical"),
function( x, shape, scale, log ) {
x <-  advector ( x )
shape <-  advector ( shape )
scale <-  advector ( scale )
log <-  as.logical ( log )
distr_dgamma ( x, shape, scale, log )
}
)
setMethod("pnorm",
signature(q = "advector_castable", mean = "advector_castable", sd = "advector_castable", lower.tail = "missing", log.p = "missing"),
function( q, mean , sd  ) {
q <-  advector ( q )
mean <-  advector ( mean )
sd <-  advector ( sd )
distr_pnorm ( q, mean , sd  )
}
)
setMethod("pgamma",
signature(q = "advector_castable", shape = "advector_castable", rate = "missing", scale = "advector_castable", lower.tail = "missing", log.p = "missing"),
function( q, shape, scale  ) {
q <-  advector ( q )
shape <-  advector ( shape )
scale <-  advector ( scale )
distr_pgamma ( q, shape, scale  )
}
)
setMethod("ppois",
signature(q = "advector_castable", lambda = "advector_castable", lower.tail = "missing", log.p = "missing"),
function( q, lambda ) {
q <-  advector ( q )
lambda <-  advector ( lambda )
distr_ppois ( q, lambda )
}
)
setMethod("pexp",
signature(q = "advector_castable", rate = "advector_castable", lower.tail = "missing", log.p = "missing"),
function( q, rate ) {
q <-  advector ( q )
rate <-  advector ( rate )
distr_pexp ( q, rate )
}
)
setMethod("pweibull",
signature(q = "advector_castable", shape = "advector_castable", scale = "advector_castable", lower.tail = "missing", log.p = "missing"),
function( q, shape, scale ) {
q <-  advector ( q )
shape <-  advector ( shape )
scale <-  advector ( scale )
distr_pweibull ( q, shape, scale )
}
)
setMethod("pbeta",
signature(q = "advector_castable", shape1 = "advector_castable", shape2 = "advector_castable", ncp = "missing", lower.tail = "missing", log.p = "missing"),
function( q, shape1, shape2 ) {
q <-  advector ( q )
shape1 <-  advector ( shape1 )
shape2 <-  advector ( shape2 )
distr_pbeta ( q, shape1, shape2 )
}
)
