PKG_CPPFLAGS=-DTMB_EIGEN_DISABLE_WARNINGS -DEIGEN_FFTW_DEFAULT -I$(LIB_FFTW)/include
PKG_LIBS=-L$(LIB_FFTW)/lib -l fftw3 $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

all: print_tmb_version $(SHLIB)

print_tmb_version:
	@Rscript -e "cat('using TMB Version:', as.character(packageVersion('TMB')), '\n')"
